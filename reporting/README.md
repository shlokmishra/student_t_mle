# Reporting

Active analysis/reporting code is under `reporting/diagnostics/`.

Canonical reference artifacts that should be kept for future audits are under:

- `reporting/diagnostic_outputs/model_reference_audit/`
- `reporting/diagnostic_outputs/kde_reference_audit/mle_sample_cache/`

Older top-level reporting scripts were moved to `_archive_repo_cleanup_20260608/legacy_reporting/`
because they targeted the archived `artifacts/final_comparison/` workflow.
